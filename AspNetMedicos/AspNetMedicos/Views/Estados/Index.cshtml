@model IEnumerable<AspNetMedicos.Models.Estados>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pais.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sigla)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pais.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sigla)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EstadosId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EstadosId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EstadosId })
        </td>
    </tr>
}

</table>
